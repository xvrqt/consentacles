/* Importetd by index.html as a module to test that TS scripts are being
 * copied and tranpiled correctly.
*/
export function helloTypeScript(greeter: string): string {
	return `Hello from example_ts.ts {greeter}!`;
}
