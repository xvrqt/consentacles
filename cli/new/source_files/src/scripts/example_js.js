/* Importetd by index.html as a module to test that JS scripts are being
 * copied and tranpiled correctly.
*/
export function helloJavaScript(greeter) {
	return `Hello from example_js.js {greeter}!`;
}
